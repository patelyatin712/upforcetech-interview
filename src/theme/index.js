import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#10ff8d',
    },
    secondary: {
      main: '#242424',
    },
    background: {
      main: '#121212',
    },
  },
});

export default theme;
